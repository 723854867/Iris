<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.busap.vcs.data.mapper.InviteRegisterDAO">
<sql id="page">limit #{pageStart},#{pageSize}</sql>
	<insert id="insert">
		insert into
		invite_register(creator_id,invite_uid,create_at,data_from)
		values(#{creatorId},#{inviteUid},#{createDate},#{dataFrom})
	</insert>
	
</mapper>